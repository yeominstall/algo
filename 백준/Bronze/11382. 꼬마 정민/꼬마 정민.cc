#include <iostream>

using namespace std;

int main(void)
{
	cin.tie(0); cout.tie(0); ios_base::sync_with_stdio(false);
	long long a, b, c;
	cin >> a >> b >> c;
	cout << a + b + c << '\n';

	return 0;
}